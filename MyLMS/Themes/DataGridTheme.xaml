<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Colori personalizzati -->
    <Color x:Key="PrimaryBackgroundColor">#FF1D1C29</Color>
    <Color x:Key="SecondaryBackgroundColor">#353340</Color>
    <Color x:Key="HoverColor">#4A4957</Color>
    <Color x:Key="SelectionColor">#5A5967</Color>
    <Color x:Key="GridLineColor">#2D2C39</Color>
    <Color x:Key="TextColor">#F1F1F1</Color>

    <SolidColorBrush x:Key="DataGrid.Background" Color="{StaticResource PrimaryBackgroundColor}"/>
    <SolidColorBrush x:Key="DataGrid.Foreground" Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="DataGrid.Header.Background" Color="{StaticResource SecondaryBackgroundColor}"/>
    <SolidColorBrush x:Key="DataGrid.Header.Foreground" Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="DataGrid.Row.Background" Color="{StaticResource PrimaryBackgroundColor}"/>
    <SolidColorBrush x:Key="DataGrid.Row.Background.Alternate" Color="#252430"/>
    <SolidColorBrush x:Key="DataGrid.Row.Selected" Color="{StaticResource SelectionColor}"/>
    <SolidColorBrush x:Key="DataGrid.Row.MouseOver" Color="{StaticResource HoverColor}"/>
    <SolidColorBrush x:Key="DataGrid.GridLines" Color="{StaticResource GridLineColor}"/>

    <!-- Stile Header (senza bordo finale) -->
    <Style x:Key="ModernDataGridColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="Background" Value="{StaticResource DataGrid.Header.Background}"/>
        <Setter Property="Foreground" Value="{StaticResource DataGrid.Header.Foreground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource DataGrid.GridLines}"/>
        <Setter Property="BorderThickness" Value="0,0,1,0"/>
        <Setter Property="Padding" Value="12,8,12,8"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                    <Grid>
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <Thumb x:Name="PART_RightHeaderGripper" 
                               HorizontalAlignment="Right" 
                               Cursor="SizeWE" 
                               Width="1" 
                               Background="Transparent"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource DataGrid.Row.MouseOver}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Stile Cella -->
    <Style x:Key="ModernDataGridCellStyle" TargetType="{x:Type DataGridCell}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource DataGrid.Foreground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource DataGrid.GridLines}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Padding" Value="12,8,12,8"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridCell}">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource DataGrid.Row.Selected}"/>
                <Setter Property="Foreground" Value="{StaticResource DataGrid.Header.Foreground}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Stile Riga SENZA il riquadro di selezione a sinistra -->
    <Style x:Key="ModernDataGridRowStyle" TargetType="{x:Type DataGridRow}">
        <Setter Property="Background" Value="{StaticResource DataGrid.Row.Background}"/>
        <Setter Property="Foreground" Value="{StaticResource DataGrid.Foreground}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridRow}">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <SelectiveScrollingGrid>
                            <SelectiveScrollingGrid.ColumnDefinitions>
                                <!-- Colonna per RowHeader RIMOSSA -->
                                <ColumnDefinition Width="*"/>
                            </SelectiveScrollingGrid.ColumnDefinitions>
                            <DataGridCellsPresenter Grid.Column="0" 
                                                    ItemsPanel="{TemplateBinding ItemsPanel}"/>
                        </SelectiveScrollingGrid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="AlternationIndex" Value="1">
                <Setter Property="Background" Value="{StaticResource DataGrid.Row.Background.Alternate}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource DataGrid.Row.MouseOver}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource DataGrid.Row.Selected}"/>
                <Setter Property="Foreground" Value="{StaticResource DataGrid.Header.Foreground}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Template DataGrid che rimuove l'area filler dell'header -->
    <ControlTemplate x:Key="ModernDataGridControlTemplate" TargetType="{x:Type DataGrid}">
        <Border Background="{TemplateBinding Background}" 
                BorderBrush="{TemplateBinding BorderBrush}" 
                BorderThickness="{TemplateBinding BorderThickness}" 
                SnapsToDevicePixels="True">
            <ScrollViewer x:Name="DG_ScrollViewer" 
                          Focusable="false"
                          Background="{TemplateBinding Background}">
                <ScrollViewer.Template>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Header senza area filler -->
                            <DataGridColumnHeadersPresenter x:Name="PART_ColumnHeadersPresenter"
                                                            Grid.Column="1"
                                                            Visibility="{Binding HeadersVisibility, 
                                                                ConverterParameter={x:Static DataGridHeadersVisibility.Column}, 
                                                                Converter={x:Static DataGrid.HeadersVisibilityConverter}, 
                                                                RelativeSource={RelativeSource AncestorType=DataGrid}}"/>

                            <!-- RowsPresenter -->
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" 
                                                    Grid.Row="1" 
                                                    Grid.Column="1" 
                                                    CanContentScroll="{TemplateBinding CanContentScroll}" />

                            <!-- ScrollBars -->
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Row="1"
                                       Grid.Column="2"
                                       Orientation="Vertical"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>

                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Grid.Row="2"
                                       Grid.Column="1"
                                       Orientation="Horizontal"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </ScrollViewer.Template>
                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
            </ScrollViewer>
        </Border>
    </ControlTemplate>

    <!-- Stile DataGrid principale -->
    <Style x:Key="ModernDataGrid" TargetType="{x:Type DataGrid}">
        <Setter Property="Background" Value="{StaticResource DataGrid.Background}"/>
        <Setter Property="Foreground" Value="{StaticResource DataGrid.Foreground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource DataGrid.GridLines}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="RowBackground" Value="{StaticResource DataGrid.Row.Background}"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource DataGrid.Row.Background.Alternate}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource DataGrid.GridLines}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource DataGrid.GridLines}"/>
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource ModernDataGridColumnHeaderStyle}"/>
        <Setter Property="CellStyle" Value="{StaticResource ModernDataGridCellStyle}"/>
        <Setter Property="RowStyle" Value="{StaticResource ModernDataGridRowStyle}"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="CanUserResizeColumns" Value="True"/>
        <Setter Property="CanUserReorderColumns" Value="True"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="EnableRowVirtualization" Value="True"/>
        <Setter Property="EnableColumnVirtualization" Value="True"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Template" Value="{StaticResource ModernDataGridControlTemplate}"/>
    </Style>

</ResourceDictionary>